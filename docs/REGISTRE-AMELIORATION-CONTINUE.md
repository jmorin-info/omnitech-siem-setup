# Registre d'amélioration continue — SIEM/XDR OMNITECH (ISO/IEC 27001:2022, clause 10)

**Périmètre :** plateforme de supervision & détection/réponse (Graylog + oms-xdr).
**Propriétaire :** RSSI (J. Morin). **Revue :** mensuelle + revue de direction (clause 9.3).
**Dernière mise à jour :** 2026-06-22.

> **Clause 10.1 (amélioration continue)** : la plateforme évolue de façon proactive
> (nouvelles sources, détections, capacités) pour améliorer en continu la pertinence
> et l'efficacité du SMSI. **Clause 10.2 (non-conformité & action corrective)** :
> distinction stricte **correction** (remise en état immédiate) vs **action corrective**
> (traitement de la cause racine) ; une entrée n'est **close qu'après vérification
> d'efficacité**. Toutes les modifications sont tracées en Git (`omnitech-siem-setup`,
> dépôt privé) — preuve de **maîtrise du changement (A.8.32)**.

## 1. Non-conformités & actions correctives (10.2)

| ID | Date | Constat (non-conformité) | Cause racine | Correction immédiate | Action corrective (cause) | Vérification d'efficacité | Annexe A | Statut |
|---|---|---|---|---|---|---|---|---|
| **AC-2026-06-18-01** | 18/06 | Tempête de ~3000 alertes/événements | (1) détection « injection de processus » trop large (taggait `bash.exe`/`node.exe` = outils dev) ; (2) **hopping window** (`search_within` 5 min > `execute_every` 1 min) → chaque message compté ×5, sur 21 définitions | Exclusion des outils dev dans la détection injection ; seuils `count≥10/8` sur injection/PowerShell/UTM | `mk_def` (13) clampe désormais `execute_every ≥ search_within` (**tumbling garanti**) ; 83 définitions repassées en tumbling | Volume d'événements : « injection » sorti du top ; vérifié post-fix (commit `104a08a`) | A.8.16 | **Clos** |
| **AC-2026-06-18-02** | 18/06 | Faux positif `exposition_internet` sur le trafic **IPsec inter-sites** (Bordeaux↔Ivry) | Règle `omni-forti-16-expo-internet` sans garde-fou : taguait tout flux entrant-WAN accepté, trafic tunnelé compris | Ajout garde-fous `subtype != "local"` **et** `NOT has_field("vpntype")` (déployé) | Garde-fous intégrés au script `49-expo-port-class.sh` (persistant) | Règle déployée vérifiée (2 garde-fous présents) ; FP n'apparaît plus sur le trafic tunnelé (commit `8897604`) | A.8.16, A.8.20 | **Clos** |
| **AC-2026-06-18-03** | 18/06 | Abort **silencieux** (exit 5) de `49-expo-port-class.sh` au re-run | Règle morte `omni-forti-15-net-direction` en syntaxe ternaire (`let = if…then…else`) **refusée par Graylog** → `api_post` échouait sous `set -e` | Retrait de la règle morte (net_direction est posé par 4 règles `dir-*`) | Documenté comme piège ; convention « 1 règle par condition, aucun conditionnel dans le `then` » | Script ré-exécuté jusqu'à `EXIT=0` (commit `8897604`) | A.8.32 | **Clos** |
| **AC-2026-06-22-01** | 22/06 | **Fuite de pseudonymisation (P0)** : en mode démo/captures (`MOBILE_REDACT`), des **noms d'hôtes/comptes réels** restaient en clair dans la narrative des incidents (12/69) | Le champ `entities` (chaîne jointe par virgules) était pseudonymisé **en bloc** → les noms individuels n'entraient pas dans la carte de correspondance, donc le scrub du texte libre ne les masquait pas | Scinder la chaîne et pseudonymiser **chaque** entité avant la passe de scrub (`omni-mobile-api.py`) | **Test de régression** verrouillant l'invariant (suite `tests/`) ; cause = mécanisme de rédaction, durci | Rejoué sur 20 incidents live : **0 fuite résiduelle** ; 24 tests verts (commit `12eae92`) | A.8.10, A.5.34 | **Clos** |
| **AC-2026-06-22-02** | 22/06 | **Allowlist FP inopérante** : table de lookup `omni-fp-allowlist` **vide** (moitié « lookup » de l'anti-FP morte ; filet `contains()` intact) | Graylog `CSVFileDataAdapter` **rejette tout le fichier** si une ligne n'a pas 2 colonnes ; les lignes de commentaire `#` (1 colonne) cassaient le CSV | Livrer un CSV **filtré** (en-tête + données), commentaires conservés en source | Filtrage **intégré au script `81`** (anti-récidive au prochain provisioning) | Lookup résout en live (`winlogbeat`→…, `adobeupdateservice`/`f5vpncleanup` OK ; reload adaptateur ~16 s) (commit `1a6acdf`) | A.8.16 | **Clos** |
| **AC-2026-06-22-03** | 22/06 | **Robustesse en mode dégradé** : `oms-ml` levait `ValueError` (au lieu du skip prévu) si OpenSearch injoignable ; `oms-xdr` pouvait planter tout un cycle sur une sévérité inconnue, et **masquait silencieusement** les règles dépendant d'un signal en échec | (ml) chemin d'erreur renvoyant un 3-uplet pour 4 valeurs déballées ; (xdr) `KeyError` sur accès direct au dict de sévérité ; signal en échec == ensemble vide indistinguable | (ml) 4-uplet ; (xdr) `.get()` défensif + **émission d'un événement `xdr_health`** quand un signal échoue (perte de couverture rendue visible) | Tripwire interne : la dégradation alimente désormais le SIEM (`event_source=xdr_health`) au lieu d'être muette | `py_compile` + 24 tests verts (commit `12eae92`) | A.8.16 | **Clos** |
| **AC-2026-06-22-04** | 22/06 | **Incohérence d'auto-supervision** : la synthèse self-health affichait « 18/18 robots OK, **1 en panne** » (contradictoire, anxiogène) | Le compteur `bad` mélangeait pannes réelles de robots et **rappels de maintenance** : un redémarrage en attente (MAJ noyau 6.12.90→6.12.94) était compté comme « robot en panne » | Séparer `maint` (reboot/MAJ) de `bad` (pannes robots/infra) ; champ `health_maint` ; `alert_tag=siem_maintenance` (≠ `siem_job_fail`) ; bandeau « Maintenance requise » distinct côté console | Cause = logique de comptage, corrigée à la source (`61-supervision-robots.sh`) | Message « 18/18 robots OK · 1 maintenance (reboot securite) » ; console : Robots 18/18 vert + bandeau (commit `1f970da`) | A.8.16 | **Clos** |
| **AC-2026-06-22-05** | 22/06 | **Reproductibilité incomplète (DR/PRA)** : 5 services prod — `omni-soar`/`omni-soar-expire` (réponse SOAR), `omni-cert-check`/`omni-cert-renew` (gestion certificats), `omni-postboot-check` (vérif post‑boot) — tournent **sans binaire NI unités systemd dans le dépôt** ; une reconstruction depuis le dépôt les omettrait silencieusement (constat émergé en fermant le gap AM-18) | Déploiement manuel historique non rapatrié ; `omni-cert-renew`/`omni-postboot-check` **sans script propriétaire** (orphelins) | Binaire **+** unités systemd capturés AS-IS : soar/soar-expire → `36-soar.sh` (avant le `enable`) ; cert-check/cert-renew/postboot-check → nouveau `86-services-versioning.sh` | Vérifié : 5 binaires *byte-identical* + 8 unités verbatim + `bash -n` (commit `4e0cf3c`) ; **20 microservices désormais reproductibles** (reconstruction PRA complète) | A.8.32, A.5.30 | **Clos** |

> **Audit interne par vérification adversariale (clause 9.2)** — les entrées **AC-2026-06-22-\*** ont été détectées par une passe d'audit multi-axes **avec réfutation** (chaque constat candidat est *contre-vérifié* par un évaluateur indépendant chargé de le réfuter avant rétention). Effet observé : 3 constats « P0/P1 » candidats ont été **revus à la baisse** (faux positifs d'audit écartés), et le « bug d'ordre de sévérité FortiGate » du backlog a été **prouvé OBSOLÈTE** (la règle `omni-forti-05-severity` est désormais un simple enrichissement par table de lookup, mono-condition, vérifié cohérent sur 84,86 M de documents — aucune action requise). La capacité du SMSI à **détecter et corriger ses propres écarts** (et à écarter les faux positifs) est elle-même une preuve d'efficacité (clause 10).

## 2. Améliorations proactives (10.1)

| ID | Date | Amélioration | Bénéfice SMSI | Annexe A | Preuve | Statut |
|---|---|---|---|---|---|---|
| AM-01 | 18/06 | **Mise sous contrôle de version** de tout le provisioning (Git privé GitHub) + gabarits de secrets | Maîtrise du changement, réduction du **risque key-person** (dépôt = source unique versionnée) | A.8.32, A.5.37 | dépôt `omnitech-siem-setup` (commits datés) | Clos |
| AM-02 | 18/06 | **Alertes auto-explicatives** : cause de l'échec, EventID, ATT&CK, score, + « explication / que faire » par détection | Réduction du MTTD/MTTR, triage plus rapide | A.8.16, A.5.25 | gabarits `13` ; lookup `alert-explain.csv` | Clos |
| AM-03 | 18/06 | **Renseignement sur les menaces étendu** : feeds IOC abuse.ch (Feodo C2, URLhaus) + matching auto | Détection des menaces **connues** (C2/domaines malveillants) | A.5.7 | `66-threatintel.sh` ; lookups `ti-*` ; tags `c2_ioc`/`malware_domain` | Clos |
| AM-04 | 18/06 | **Couverture ATT&CK étendue** : web shell (T1505.003), NTDS (T1003.003), reco AD (T1087.002) + carte de couverture | Comblement de trous de détection priorisés ; preuve de couverture | A.8.16, A.5.7 | `67-detection-coverage.sh` ; `docs/mitre-navigator-layer.json` | Clos |
| AM-05 | 18/06 | **Couche XDR (oms-xdr)** : corrélation kill-chain + triage LLM local (Ollama) + réponse dry-run | Qualification/priorisation des incidents, narration analyste | A.5.24, A.5.25 | `oms-xdr/` ; timer `oms-xdr` ; page dashboard « OMS-XDR » | Clos |
| AM-06 | 18/06 | **Nouvelle source FortiManager** (admin/config) + **app mobile PWA** (alertes/incidents + push) | Couverture journalisation FMG ; réponse RSSI en mobilité | A.8.15, A.5.26 | `63-fortimanager.sh` ; `65-mobile-pwa.sh` | En cours (forwarding FMG / egress push côté infra) |
| AM-07 | 18/06 | **Actionneur de réponse AD** : désactivation de compte compromis via LDAPS (dry-run + denylist + audit + human-in-the-loop) | Neutralisation rapide d'un compte compromis | A.5.26, A.8.16 | `69-ad-response.sh` ; `/usr/local/sbin/omni-ad-disable` | Dry-run (à armer après délégation AD) |
| AM-08 | 22/06 | **Couche d'apprentissage (oms-ml)** : détection d'anomalie non-supervisée par entité (IsolationForest) + score `ml_score` 0-100 réinjecté ; segmentation v2 par classe d'actif + amortissement du score | Hiérarchisation des entités à risque au-delà des règles déterministes | A.8.16, A.5.7 | `77-ml-scoring.sh` (déployé) ; timers `oms-ml-anomaly`/`oms-ml-fp` ; `event_source=ml_anomaly` | Anomalie en prod ; **réduction FP supervisée en attente de labels VP/FP** |
| AM-09 | 22/06 | **Correction d'angle mort majeur** : index set dédié `omni-interne` + rattachement du flux interne | `ueba_score` (74k), `ml_anomaly`, `siem_health`, `xdr_incident`, `collecte_sla` étaient dans `graylog_0`, **invisibles à la console** (qui lit `omni-*`) — désormais visibles/exploitables | A.8.15, A.8.16 | `79-interne-indexset.sh` (déployé) | Clos (vérifié : flux reçus) |
| AM-10 | 22/06 | **Réduction de bruit mesurée (allowlist FP)** : pipeline stage 25 qui, pour motifs bénins **mesurés** (scheduled_task 97 % FP, service_install 86 % FP, dont nos propres agents winlogbeat/Sysmon), pose `fp_allowlist=true` et **retire `alert_tag`** — l'événement reste indexé (visibilité conservée) | Moins d'alertes bénignes, triage plus net ; **réversible** (lookup CSV) | A.8.16 | `81-fp-allowlist.sh` (déployé) ; `lookups/fp-allowlist.csv` (16 motifs) | Clos (réversible) |
| AM-11 | 22/06 | **Dashboard « OMNI - Analytics »** : 5 onglets (vue d'ensemble, anomalies ML, UEBA, couverture & santé, bruit/FP) | Pilotage SOC : couverture, santé des robots/SLA, hiérarchie de risque, mesure du bruit | A.8.16, A.5.25 | `82-graylog-analytics-dashboard.sh` (déployé) | Clos |
| AM-12 | 22/06 | **Corrélation kill-chain (oms-xdr)** : 4 règles multi-signaux (vol LSASS→persistance, PowerShell offensif→persistance, usage de creds→partage admin, LSASS→latéral 6 h) + fenêtre par signal | Détection de **séquences** d'attaque (au-delà de l'alerte isolée) ; additif, dry-run de réponse préservé | A.8.16, A.5.25 | `oms-xdr/oms_xdr/rules.yaml` (25 signaux/16 règles) ; timer `oms-xdr` | Clos (live via timer) |
| AM-13 | 22/06 | **Console SOC enrichie + mode rédaction** : cartes ML/UEBA (scores réels), sévérité `risk_severity`, graphe filtrable, tendances KPI, Entité-360, recherche/export, disposition VP/FP ; **mode `MOBILE_REDACT`** (pseudonymisation comptes/IP/SID/hôtes pour captures) ; UI professionnalisée (icônes SVG, neon atténué) | Triage RSSI en mobilité ; preuve de capacité **sans exposer de données réelles** (captures anonymisées) | A.5.25, A.8.16, A.8.10 | `mobile/` ; `docs/captures/` ; galerie README | Clos |
| AM-14 | 22/06 | **Consolidation des alertes Kerberos (anti-bruit)** : kerberoasting/RC4 ×3 et AS-REP ×2 sur le même événement → **5 alertes ramenées à 2** (source canonique = `73`), sans perte de détection | Suppression d'un sur-alertage **latent** (posture AES confirmée : zéro RC4/0x17 en 90 j) | A.8.16 | `84-kerberoast-dedup.sh` ; garde-fou anti-recréation dans `13` | **Clos** (appliqué 22/06) |
| AM-15 | 22/06 | **Modèle FP supervisé — features contextuelles (anti-fuite)** : ajout de 3 features d'historique par entité (volume, routine du tag, diversité) pour distinguer VP/FP *à l'intérieur* d'un même `alert_tag` ; régularisation faible volume | Le modèle peut apprendre le **contexte** (ce que la allowlist déterministe ne fait pas) au lieu de re-coder `tag→FP` ; prêt pour des labels de qualité dès qu'ils s'accumulent | A.8.16, A.5.7 | `oms-ml/oms_ml/fpscore.py` ; 41 tests (dont contexte) | Améliorations livrées ; **entraînement toujours en attente de ≥30 cas qualifiés** (gating volontaire) |
| AM-16 | 22/06 | **Audit de précision/FP par vérification adversariale (~0 % erreur visé)** : 7 détections fortement bruitées mesurées puis resserrées avec garde-fou **contre-vérifié** — `vsphere_auth_fail` (~99,6 % FP : blobs de télémétrie vpxd matchant « authentication »+« fail » sur des noms de classe), `malware_domain` (~100 % : **qualité TI**, infra partagée type github/discord écartée du lookup DNS), `persistence_autorun`, `admin_share` (loopback C$ ≠ latéral), `m365_partage_externe` (CompanyLink = interne), `network_scan` (NMS SNMP), `powershell_suspect` (Claude Code, **go RSSI**) | Plusieurs **milliers de FP/sem.** supprimés **sans perte de vrai positif** (chaque correctif réfuté avant rétention) | A.8.16 | commits `19`/`66`/`81`/`18`/`48`/`00-vars.env` (22/06) | **Clos** |
| AM-17 | 22/06 | **Guides d'action sur 100 % des détections actives** : +13 playbooks 4 parties (comportementales/NDR/ML/vuln/internes) → **0 détection qui tire sans guide** ; + **page Entités dédiée** (recherche tout compte/machine), **dossier 360°**, **chronologie unifiée**, **corrélation d'identité** (comptes liés agrégés), **i18n FR/EN** | Triage **guidé de bout en bout** ; investigation entité unifiée sur tout le parc | A.5.25, A.8.16 | `lookups/alert-guidance.json` (83 playbooks) ; `mobile/` (commits 22/06) | **Clos** |
| AM-18 | 22/06 | **Reproductibilité des détecteurs (fermeture du risque key-person)** : **15 microservices** de détection/jobs (NDR scan/exfil/beacon/lateral/dns + UEBA volume/geo/score/newcountry + vuln-scan/geo-flux/incident-correlate/ldap-recon/monthly/weekly-report) tournaient **sans source dans le dépôt** (le provisioning se contentait de `die`-checker leur présence) → versionnés AS-IS (heredoc *byte-identical* au binaire) + resserrement structurel SNMP du scan | Détection-as-code reproductible/auditable | A.8.32, A.5.37 | `34`/`38`/`40`/`42`/`43`/`44`/`45`/`48`/`49`/`50`/`51` (commits 22/06) | **Clos (15)** |
| AM-19 | 23/06 | **Comblement des tactiques minces de la carte ATT&CK** : la carte de couverture montrait Privilege Escalation et Lateral Movement à **3 techniques** chacune (axe le plus perfectible). Ajout de **3 détecteurs haute-fidélité** dont la signature offensive cadrée mesure **0 sur 30 j** *alors que le champ porteur est vivant (contrôle positif)* : `ifeo_debugger` (T1574.012 — valeur Debugger sous Image File Execution Options ; racine IFEO active à 2088 écritures légitimes/30j, `\Debugger`=0), `service_host_shell` (T1068 — spoolsv/services→shell ; PrintNightmare/abus SCM ; spoolsv-parent vivant à 8 hits légitimes), `winrm_lateral` (T1021.006 — wsmprovhost→recon, powershell exclu). **2 faux candidats écartés par la mesure** : named-pipe PsExec (Sysmon EID17=1 event → infaisable) et process PSEXESVC (usage admin plausible → FP). **PrivEsc 3→5, Lateral Movement 3→4** ; 90 détections / 59 techniques / 13-14 tactiques (seul trou : Resource Development, structurellement hors-télémétrie) | Couverture kill-chain renforcée **sans dette FP** (chaque règle 0-baseline + champ validé) ; +3 playbooks 4-parties (92 au total) | A.8.16, A.8.7 | `87-detection-extra4.sh` ; `lookups/mitre-attack.csv` ; `lookups/alert-guidance.json` ; `docs/mitre-navigator-layer.json` (57 relancé) | **Clos (3)** |

> **Décision d'ingénierie (jugement, 22/06) — modèle FP supervisé NON entraîné :** avec
> **3 cas / 1 label**, l'entraînement est volontairement bloqué (gating `fpscore`).
> *Fabriquer des labels pour atteindre le seuil serait un empoisonnement du jeu
> d'entraînement* — un faux « FP » apprendrait au modèle à **supprimer une vraie
> détection**. Les labels doivent provenir de la **qualification réelle** des cas par
> l'analyste (disposition VP/FP à la clôture, console SOC). L'effort porte donc sur la
> **qualité des features** (ci-dessus), pas sur le volume artificiel.

> **Décisions en attente de feu vert RSSI (revue de direction / changement maîtrisé A.8.32)** — chaque action est *préparée, testée en dry-run, et réversible ou à impact immédiat nul* ; elle n'est exécutée qu'après décision tracée :
> - **DEC-01 — Rétention consolidée** : `APPLY=1 ./83-retention-consolidee.sh`. Établit la source unique de rétention (valeurs = `POLITIQUE-RETENTION.md`) **et** corrige le routage `OMNI - FortiManager` (graylog_0 → `omni-fortimanager`). *Impact immédiat : 0 index supprimé (dry-run vérifié) ; à terme, purge automatique au-delà des seuils (comportement voulu).* Irréversible (auto-suppression). A.8.10.
> - **DEC-02 — Dédoublonnage Kerberos** : `APPLY=1 ./84-kerberoast-dedup.sh`. Supprime 3 définitions d'alerte redondantes (garde-fou : aucune suppression si l'alerte canonique du cluster est absente). *Réversible (ré-exécuter `73`).* A.8.16.
> - **DEC-03 — Tripwires 0-FP supplémentaires** : déployer `80-detection-extra2.sh` (defender_tamper, schtask_payload, amsi_bypass) puis relancer `57` (carte couverture) + `14` (dashboards). *Additif (nouvelles détections), pas de suppression.* A.8.16.

> **Risque accepté (décision RSSI, 18/06) — AC-RA-01 :** réutilisation du compte
> d'authentification LDAPS `svc_siem` comme acteur de désactivation AD, plutôt qu'un
> compte de réponse dédié. **Écart signalé** au moindre privilège (A.5.18) et à la
> séparation des tâches : un secret d'authentification très exposé acquiert une
> capacité d'écriture sur AD. **Mesures compensatoires en place :** denylist stricte
> des comptes protégés (admin/service/secours/`krbtgt`/`adm-`/`svc_`) ; exécution
> **dry-run par défaut** (double verrou `OMNI_AD_DISABLE_ARM`) ; **journalisation GELF**
> de chaque tentative (`event_source=ad_response`) ; invocation **human-in-the-loop**
> uniquement (jamais en réponse automatique) ; secret LDAPS **non dupliqué** (lu depuis
> `00-vars.env`). **À réévaluer en revue de direction** (bascule vers un compte dédié recommandée).

## 3. Méthode (attendue par l'auditeur Stage 2, nov. 2026)
- Chaque entrée porte **date, propriétaire, cause racine, action, et preuve de vérification** ; la preuve est le commit Git + l'état vérifié en production (« evidence is evidence »).
- Les entrées ci-dessus alimentent la **revue de direction** (clause 9.3) et le **registre des risques**.
- Indicateur de vitalité du SMSI : ce registre montre des entrées **datées et vérifiées** (≠ scores figés d'une année sur l'autre = drapeau rouge auditeur).
