# Continual Improvement Register — SIEM/XDR OMNITECH (ISO/IEC 27001:2022, clause 10)

**Scope:** monitoring & detection/response platform (Graylog + oms-xdr).
**Owner:** CISO (J. Morin). **Review:** monthly + management review (clause 9.3).
**Last updated:** 2026-06-22.

> **Clause 10.1 (continual improvement)**: the platform evolves proactively
> (new sources, detections, capabilities) to continually improve the relevance
> and effectiveness of the ISMS. **Clause 10.2 (nonconformity & corrective action)**:
> strict distinction between **correction** (immediate restoration) and **corrective action**
> (treatment of the root cause); an entry is **closed only after effectiveness verification**.
> All changes are tracked in Git (`omnitech-siem-setup`,
> private repository) — evidence of **change control (A.8.32)**.

## 1. Nonconformities & corrective actions (10.2)

| ID | Date | Finding (nonconformity) | Root cause | Immediate correction | Corrective action (cause) | Effectiveness verification | Annex A | Status |
|---|---|---|---|---|---|---|---|---|
| **AC-2026-06-18-01** | 18/06 | Storm of ~3000 alerts/events | (1) "process injection" detection too broad (tagged `bash.exe`/`node.exe` = dev tools); (2) **hopping window** (`search_within` 5 min > `execute_every` 1 min) → each message counted ×5, across 21 definitions | Exclusion of dev tools in the injection detection; thresholds `count≥10/8` on injection/PowerShell/UTM | `mk_def` (13) now clamps `execute_every ≥ search_within` (**tumbling guaranteed**); 83 definitions reset to tumbling | Event volume: "injection" out of the top; verified post-fix (commit `104a08a`) | A.8.16 | **Closed** |
| **AC-2026-06-18-02** | 18/06 | False positive `exposition_internet` on **inter-site IPsec** traffic (Bordeaux↔Ivry) | Rule `omni-forti-16-expo-internet` without a safeguard: tagged every accepted inbound-WAN flow, tunneled traffic included | Added safeguards `subtype != "local"` **and** `NOT has_field("vpntype")` (deployed) | Safeguards integrated into script `49-expo-port-class.sh` (persistent) | Deployed rule verified (2 safeguards present); FP no longer appears on tunneled traffic (commit `8897604`) | A.8.16, A.8.20 | **Closed** |
| **AC-2026-06-18-03** | 18/06 | **Silent** abort (exit 5) of `49-expo-port-class.sh` on re-run | Dead rule `omni-forti-15-net-direction` in ternary syntax (`let = if…then…else`) **rejected by Graylog** → `api_post` failed under `set -e` | Removal of the dead rule (net_direction is set by 4 `dir-*` rules) | Documented as a pitfall; convention "1 rule per condition, no conditional in the `then`" | Script re-run through to `EXIT=0` (commit `8897604`) | A.8.32 | **Closed** |
| **AC-2026-06-22-01** | 22/06 | **Pseudonymization leak (P0)**: in demo/screenshot mode (`MOBILE_REDACT`), **real hostnames/accounts** remained in clear text in the incident narrative (12/69) | The `entities` field (comma-joined string) was pseudonymized **as a whole** → individual names did not enter the correspondence map, so the free-text scrub did not mask them | Split the string and pseudonymize **each** entity before the scrub pass (`omni-mobile-api.py`) | **Regression test** locking the invariant (`tests/` suite); cause = redaction mechanism, hardened | Replayed on 20 live incidents: **0 residual leak**; 24 tests green (commit `12eae92`) | A.8.10, A.5.34 | **Closed** |
| **AC-2026-06-22-02** | 22/06 | **Inoperative FP allowlist**: lookup table `omni-fp-allowlist` **empty** (the "lookup" half of the anti-FP dead; `contains()` net intact) | Graylog `CSVFileDataAdapter` **rejects the entire file** if a line does not have 2 columns; the `#` comment lines (1 column) broke the CSV | Deliver a **filtered** CSV (header + data), comments preserved in source | Filtering **integrated into script `81`** (recurrence prevention at the next provisioning) | Lookup resolves live (`winlogbeat`→…, `adobeupdateservice`/`f5vpncleanup` OK; adapter reload ~16 s) (commit `1a6acdf`) | A.8.16 | **Closed** |
| **AC-2026-06-22-03** | 22/06 | **Robustness in degraded mode**: `oms-ml` raised `ValueError` (instead of the intended skip) if OpenSearch was unreachable; `oms-xdr` could crash an entire cycle on an unknown severity, and **silently masked** rules depending on a failed signal | (ml) error path returning a 3-tuple for 4 unpacked values; (xdr) `KeyError` on direct access to the severity dict; a failed signal == empty set, indistinguishable | (ml) 4-tuple; (xdr) defensive `.get()` + **emission of an `xdr_health` event** when a signal fails (coverage loss made visible) | Internal tripwire: degradation now feeds the SIEM (`event_source=xdr_health`) instead of being silent | `py_compile` + 24 tests green (commit `12eae92`) | A.8.16 | **Closed** |
| **AC-2026-06-22-04** | 22/06 | **Self-monitoring inconsistency**: the self-health summary displayed "18/18 robots OK, **1 down**" (contradictory, alarming) | The `bad` counter mixed real robot failures and **maintenance reminders**: a pending reboot (kernel update 6.12.90→6.12.94) was counted as a "robot down" | Separate `maint` (reboot/update) from `bad` (robot/infra failures); `health_maint` field; `alert_tag=siem_maintenance` (≠ `siem_job_fail`); distinct "Maintenance required" banner on the console side | Cause = counting logic, fixed at the source (`61-supervision-robots.sh`) | Message "18/18 robots OK · 1 maintenance (security reboot)"; console: Robots 18/18 green + banner (commit `1f970da`) | A.8.16 | **Closed** |
| **AC-2026-06-22-05** | 22/06 | **Incomplete reproducibility (DR/BCP)**: 5 prod services — `omni-soar`/`omni-soar-expire` (SOAR response), `omni-cert-check`/`omni-cert-renew` (certificate management), `omni-postboot-check` (post-boot check) — run **without a binary NOR systemd units in the repository**; a rebuild from the repository would silently omit them (finding that emerged while closing the AM-18 gap) | Historical manual deployment not brought back in; `omni-cert-renew`/`omni-postboot-check` **without an owner script** (orphans) | Binary **+** systemd units captured AS-IS: soar/soar-expire → `36-soar.sh` (before the `enable`); cert-check/cert-renew/postboot-check → new `86-services-versioning.sh` | Verified: 5 *byte-identical* binaries + 8 verbatim units + `bash -n` (commit `4e0cf3c`); **20 microservices now reproducible** (complete BCP rebuild) | A.8.32, A.5.30 | **Closed** |

> **Internal audit by adversarial verification (clause 9.2)** — the **AC-2026-06-22-\*** entries were detected by a multi-axis audit pass **with refutation** (each candidate finding is *counter-verified* by an independent evaluator tasked with refuting it before retention). Observed effect: 3 candidate "P0/P1" findings were **downgraded** (audit false positives dismissed), and the backlog's "FortiGate severity ordering bug" was **proven OBSOLETE** (the rule `omni-forti-05-severity` is now a simple lookup-table enrichment, single-condition, verified consistent across 84.86 M documents — no action required). The ISMS's ability to **detect and correct its own deviations** (and to dismiss false positives) is itself evidence of effectiveness (clause 10).

## 2. Proactive improvements (10.1)

| ID | Date | Improvement | ISMS benefit | Annex A | Evidence | Status |
|---|---|---|---|---|---|---|
| AM-01 | 18/06 | **Version control** of all provisioning (private GitHub Git) + secrets templates | Change control, reduction of **key-person risk** (repository = single versioned source) | A.8.32, A.5.37 | repository `omnitech-siem-setup` (dated commits) | Closed |
| AM-02 | 18/06 | **Self-explanatory alerts**: cause of the failure, EventID, ATT&CK, score, + "explanation / what to do" per detection | Reduced MTTD/MTTR, faster triage | A.8.16, A.5.25 | templates `13`; lookup `alert-explain.csv` | Closed |
| AM-03 | 18/06 | **Extended threat intelligence**: abuse.ch IOC feeds (Feodo C2, URLhaus) + automatic matching | Detection of **known** threats (C2/malicious domains) | A.5.7 | `66-threatintel.sh`; lookups `ti-*`; tags `c2_ioc`/`malware_domain` | Closed |
| AM-04 | 18/06 | **Extended ATT&CK coverage**: web shell (T1505.003), NTDS (T1003.003), AD recon (T1087.002) + coverage map | Filling of prioritized detection gaps; coverage evidence | A.8.16, A.5.7 | `67-detection-coverage.sh`; `docs/mitre-navigator-layer.json` | Closed |
| AM-05 | 18/06 | **XDR layer (oms-xdr)**: kill-chain correlation + local LLM triage (Ollama) + dry-run response | Incident qualification/prioritization, analyst narrative | A.5.24, A.5.25 | `oms-xdr/`; timer `oms-xdr`; "OMS-XDR" dashboard page | Closed |
| AM-06 | 18/06 | **New FortiManager source** (admin/config) + **mobile PWA app** (alerts/incidents + push) | FMG logging coverage; CISO response on the move | A.8.15, A.5.26 | `63-fortimanager.sh`; `65-mobile-pwa.sh` | In progress (FMG forwarding / egress push on the infra side) |
| AM-07 | 18/06 | **AD response actuator**: disabling of a compromised account via LDAPS (dry-run + denylist + audit + human-in-the-loop) | Rapid neutralization of a compromised account | A.5.26, A.8.16 | `69-ad-response.sh`; `/usr/local/sbin/omni-ad-disable` | Dry-run (to be armed after AD delegation) |
| AM-08 | 22/06 | **Learning layer (oms-ml)**: unsupervised per-entity anomaly detection (IsolationForest) + `ml_score` 0-100 re-injected; segmentation v2 by asset class + score decay | Ranking of at-risk entities beyond deterministic rules | A.8.16, A.5.7 | `77-ml-scoring.sh` (deployed); timers `oms-ml-anomaly`/`oms-ml-fp`; `event_source=ml_anomaly` | Anomaly in prod; **supervised FP reduction awaiting TP/FP labels** |
| AM-09 | 22/06 | **Major blind-spot fix**: dedicated index set `omni-interne` + attachment of the internal stream | `ueba_score` (74k), `ml_anomaly`, `siem_health`, `xdr_incident`, `collecte_sla` were in `graylog_0`, **invisible to the console** (which reads `omni-*`) — now visible/usable | A.8.15, A.8.16 | `79-interne-indexset.sh` (deployed) | Closed (verified: streams received) |
| AM-10 | 22/06 | **Measured noise reduction (FP allowlist)**: pipeline stage 25 which, for **measured** benign patterns (scheduled_task 97% FP, service_install 86% FP, including our own winlogbeat/Sysmon agents), sets `fp_allowlist=true` and **removes `alert_tag`** — the event remains indexed (visibility preserved) | Fewer benign alerts, cleaner triage; **reversible** (CSV lookup) | A.8.16 | `81-fp-allowlist.sh` (deployed); `lookups/fp-allowlist.csv` (16 patterns) | Closed (reversible) |
| AM-11 | 22/06 | **"OMNI - Analytics" dashboard**: 5 tabs (overview, ML anomalies, UEBA, coverage & health, noise/FP) | SOC steering: coverage, robot/SLA health, risk ranking, noise measurement | A.8.16, A.5.25 | `82-graylog-analytics-dashboard.sh` (deployed) | Closed |
| AM-12 | 22/06 | **Kill-chain correlation (oms-xdr)**: 4 multi-signal rules (LSASS theft→persistence, offensive PowerShell→persistence, cred use→admin share, LSASS→lateral 6 h) + per-signal window | Detection of attack **sequences** (beyond the isolated alert); additive, response dry-run preserved | A.8.16, A.5.25 | `oms-xdr/oms_xdr/rules.yaml` (25 signals/16 rules); timer `oms-xdr` | Closed (live via timer) |
| AM-13 | 22/06 | **Enriched SOC console + redaction mode**: ML/UEBA cards (real scores), `risk_severity` severity, filterable graph, KPI trends, Entity-360, search/export, TP/FP disposition; **`MOBILE_REDACT` mode** (pseudonymization of accounts/IP/SID/hosts for screenshots); professionalized UI (SVG icons, toned-down neon) | CISO triage on the move; capability evidence **without exposing real data** (anonymized screenshots) | A.5.25, A.8.16, A.8.10 | `mobile/`; `docs/captures/`; README gallery | Closed |
| AM-14 | 22/06 | **Kerberos alert consolidation (anti-noise)**: kerberoasting/RC4 ×3 and AS-REP ×2 on the same event → **5 alerts reduced to 2** (canonical source = `73`), with no loss of detection | Removal of **latent** over-alerting (AES posture confirmed: zero RC4/0x17 in 90 d) | A.8.16 | `84-kerberoast-dedup.sh`; anti-recreation safeguard in `13` | **Closed** (applied 22/06) |
| AM-15 | 22/06 | **Supervised FP model — contextual features (anti-leak)**: added 3 per-entity history features (volume, tag routine, diversity) to distinguish TP/FP *within* a single `alert_tag`; low-volume regularization | The model can learn **context** (which the deterministic allowlist does not do) instead of re-coding `tag→FP`; ready for quality labels as soon as they accumulate | A.8.16, A.5.7 | `oms-ml/oms_ml/fpscore.py`; 41 tests (including context) | Improvements delivered; **training still awaiting ≥30 qualified cases** (deliberate gating) |
| AM-16 | 22/06 | **Precision/FP audit by adversarial verification (~0% error targeted)**: 7 heavily noisy detections measured then tightened with a **counter-verified** safeguard — `vsphere_auth_fail` (~99.6% FP: vpxd telemetry blobs matching "authentication"+"fail" on class names), `malware_domain` (~100%: **TI quality**, shared infra such as github/discord removed from the DNS lookup), `persistence_autorun`, `admin_share` (loopback C$ ≠ lateral), `m365_partage_externe` (CompanyLink = internal), `network_scan` (SNMP NMS), `powershell_suspect` (Claude Code, **CISO go-ahead**) | Several **thousand FP/week** removed **without loss of true positive** (each fix refuted before retention) | A.8.16 | commits `19`/`66`/`81`/`18`/`48`/`00-vars.env` (22/06) | **Closed** |
| AM-17 | 22/06 | **Action guides on 100% of active detections**: +13 four-part playbooks (behavioral/NDR/ML/vuln/internal) → **0 detection firing without a guide**; + **dedicated Entities page** (search any account/machine), **360° dossier**, **unified timeline**, **identity correlation** (linked accounts aggregated), **i18n FR/EN** | **End-to-end guided** triage; unified entity investigation across the whole estate | A.5.25, A.8.16 | `lookups/alert-guidance.json` (83 playbooks); `mobile/` (commits 22/06) | **Closed** |
| AM-18 | 22/06 | **Detector reproducibility (closing key-person risk)**: **15 detection/job microservices** (NDR scan/exfil/beacon/lateral/dns + UEBA volume/geo/score/newcountry + vuln-scan/geo-flux/incident-correlate/ldap-recon/monthly/weekly-report) ran **without source in the repository** (provisioning merely `die`-checked their presence) → versioned AS-IS (heredoc *byte-identical* to the binary) + structural SNMP tightening of the scan | Reproducible/auditable detection-as-code | A.8.32, A.5.37 | `34`/`38`/`40`/`42`/`43`/`44`/`45`/`48`/`49`/`50`/`51` (commits 22/06) | **Closed (15)** |
| AM-24 | 23/06 | **Integration of 4 new sources (DNS / Linux / Aruba / Entra)** — measure-first method + ultracode workflow (4 agents) + verification by injection. **DNS** (Windows DNS on the DC, DNSServer/Audit channel already collected): `91-dns.sh` detects manual record changes (515/516 AUDIT_REC_ADMIN), sensitive names (wpad/autodiscover/_msdcs = MITM hijack), cache flush (536) — 519/520 noise (dynamic update) excluded; **real case found**: deletion of the A record `vaultwarden.omnitech-security.fr`. **Linux** (Debian servers): `92-linux.sh` = Syslog TCP 1519 input + grok parse of sshd/sudo/useradd + 4 detections (ssh_fail/sudo_root/user_added/sensitive_tamper) + aggregated brute-force alert + **`kit/linux-omni.sh` kit** (rsyslog+auditd). **Aruba** (ArubaOS switches): `93-aruba.sh` = Syslog UDP+TCP 1520 input + 4 detections (auth_fail/config_change/port_security/admin_login) + switch config. **Entra** (signin/audit already collected via the M365 fetcher): `94-entra.sh` closes 2 gaps (m365_app_credential_add cloud backdoor T1098.001; m365_ca_change Conditional Access T1556.009) + `docs/ENTRA-SETUP.md` (tenant action: P2 license + Graph permission to unlock riskDetections; blocking legacy auth = **1461 SMTP measured**). **2 Graylog gotchas fixed**: (a) a "match either" stage only continues to the next if ≥1 rule matches → base+parse at the same stage 0; (b) a pipeline must start at stage 0. **All verified end-to-end by injection.** +14 detections, +13 playbooks (111) | Extended coverage: DNS resolver, Linux servers, Aruba network, Entra cloud identity | A.8.15, A.8.16, A.8.7 | `91`/`92`/`93`/`94`; `kit/linux-omni.sh`; `06-firewall.sh`; `docs/ENTRA-SETUP.md` | **Closed (DNS/Entra on real data; Linux/Aruba receivers ready → deploy kit/switch config)** |
| AM-23 | 23/06 | **FortiClient EMS integration (endpoint telemetry)**: new source — estate endpoints via FortiClient EMS. **Blocker found by measurement**: the EMS (10.33.80.15) sends **ENCRYPTED syslog (TLS)** to 514 (cleartext input → gibberish, TLS encryption bytes identified) → input recreated as **TLS** (self-signed cert accepted by the EMS) + redirect 514→1518 (ESET mirror, persisted 06-firewall.sh/00-vars). Format confirmed on **real data** (key=value FortiGate-style). `90-fortiems.sh` (idempotent): cert+TLS input, index set `omni-fortiems`, stream (route gl2_source_input), pipeline (proven **key_value** parse + full `msg=` recovery via grok + level→risk_severity normalization + host) + **3 detections** (forticlient_malware/vuln/av_off) + **3 mail+Teams alerts** + MITRE. **oms-xdr** correlation (`CR_ENDPOINT_MALWARE`). Console **"Endpoint (EMS)" tab** + **per-host 360° dossier block** + **PWA section**. **+3 playbooks** (98 — "0 detection without a guide" upheld). **PITFALL fixed**: `terms` agg on `level` (text field→fielddata disabled→`os_search` returned `{}`→silent 0) removed. **VERIFIED end-to-end by TLS injection** (EICAR → parse → risk_severity=critical → forticlient_malware fires → propagates up to the host's dossier) | Endpoint visibility (AV/vuln/tamper) integrated into the detection+correlation+response chain | A.8.7, A.8.16, A.8.8 | `90-fortiems.sh`; `06-firewall.sh`; `oms-xdr/`; `mobile/`; `lookups/` | **Closed (ingestion verified; detections to refine on the 1st real security event)** |
| AM-22 | 23/06 | **OMNI Sentinel Pillar 3 — Orchestrated graduated response (human-gated, defensive SOAR)**: composes a **triggered decoy** (Pillar 1) with the **attack-twin context** (Pillar 2: blast radius, distance to the crown jewels, chokepoint) → a **graded response plan** (critical/high/moderate). Full loop: **detect → contextualize → respond**. CISO decision = **armed execution on OMNITECH's own infra** (NinjaOne isolation, FortiGate blocking via the omni-soar feed). **Safeguards by construction (quadruple lock)**: DRY-RUN by default + `auto_<action>=true` + env `OMNI_SENTINEL_ARM=1` + explicit `--execute` approval (never automatic); **identity (AD) actions ALWAYS a recommendation** (never armed); **co-managed (invissys) target forced dry-run** in hard code. GELF audit `event_source=sentinel_response`. Timer `oms-graph-respond` (15 min) grades+audits **without ever executing**. Demonstrated in tabletop (`--simulate iv-tech-fmo-lt` → CRITICAL, 4 crown jewels; `adm-jmorin` → account actions); **11 tests green** (grade/scope/double-lock/co-managed). Reuses the dry-run primitives of oms-xdr/responder | Proactive response under strict control; the 3 pillars form a system | A.5.26, A.8.16, A.5.7 | `oms-graph/oms_graph/response.py`; `89` (respond timer) | **Closed (engine + safeguards; arming = Julien's gesture)** |
| AM-21 | 23/06 | **OMNI Sentinel Pillar 2 — Attack twin / exposure analysis (`oms-graph`)**: **passively** reconstructs (without an AD probe, from the already-collected logon telemetry) an exposure graph and puts it in the service of **defense** — prioritize hardening and **pre-position the decoys** (Pillar 1). Edges: HasSession (4624 LogonType 2/10/11 = credentials exposed on the host) + AdminTo (4672 = admin of the host). Computes: **crown jewel** exposure (who reaches them, in how many hops), **chokepoints** (where to harden/decoy), **blast radius**, catastrophic **single points**, **decoy placement recommendations**. Measured anti-noise: machine/system/virtual accounts (DWM/UMFD/MSSQL$/IUSR) excluded → 204→**109 accounts**; ubiquitous management accounts (ninjaone admin of 42 hosts) removed from lateral paths and reported separately. Real findings on the estate: DC reachable in ≤4 hops, `ninjaone` = catastrophic single point, the `adm-*` reach all the crown jewels. Python package (requests+PyYAML, hand-coded graph, **5 tests green**) modeled on oms-ml; daily timer; outputs = console JSON artifact + GELF `event_source=attack_path` **informational (no alert_tag** = posture, not a recurring alert). **Design NB**: the ultracode workflow was blocked by the cyber classifier ("attack path" language) → run inline in explicit defensive framing | Proactive defense: see and close the paths **before** the attacker; feeds decoy placement | A.8.7, A.8.16, A.5.7 | `oms-graph/`; `89-attack-graph.sh` | **Closed (engine deployed)** |
| AM-20 | 23/06 | **OMNI Sentinel — proactive DECEPTION layer (0-FP honeytokens)**: shift from *reactive* detection (waiting for the signature) to *proactive* (fabricating the trap). Decoys with no legitimate use whatsoever — AD accounts/machine accounts, Kerberoast SPNs, canary FQDNs embedded in bait files — placed on the **attack paths** to the crown jewels (DC/SIEM/Veeam/Vault/PKI/files/vSphere). Designed+measured+**adversarially verified** (15-agent workflow): **collision = 0 over 30 d** for the 15 accounts + 5 FQDNs (TargetUserName/ServiceName/QueryName) → 0-FP *by construction*, ~100% fidelity. Extensible registry (lookup `omni-deception`, +1 CSV line = covered in <60 s). 5 multi-source rules (decoy_identity T1078, decoy_kerberoast T1558.003 *zero_fp verified — the strongest*, canary_token T1005). Honest verdicts: decoy_host **dropped** (EID3 = scan noise → folded into identity via machine accounts); decoy_file **dropped** (no read auditing) → embedded canary; canary **revised** to external domains disjoint from the AD zone. The **bait** = Julien dry-run action on OMNITECH AD (never the co-managed tenant) | Pre-impact capture; +3 playbooks (95); Collection 2→3; proactive detection-as-code | A.8.16, A.8.7, A.5.7 | `88-deception-honeytokens.sh`; `lookups/deception-decoys.csv`; `docs/DECEPTION-PLAN.md`; `lookups/alert-guidance.json`; `lookups/mitre-attack.csv` | **Closed (detection armed); bait awaiting Julien** |
| AM-19 | 23/06 | **Filling of thin tactics on the ATT&CK map**: the coverage map showed Privilege Escalation and Lateral Movement at **3 techniques** each (the most improvable axis). Added **3 high-fidelity detectors** whose framed offensive signature measures **0 over 30 d** *while the carrying field is alive (positive control)*: `ifeo_debugger` (T1574.012 — Debugger value under Image File Execution Options; IFEO root active at 2088 legitimate writes/30d, `\Debugger`=0), `service_host_shell` (T1068 — spoolsv/services→shell; PrintNightmare/SCM abuse; spoolsv-parent alive at 8 legitimate hits), `winrm_lateral` (T1021.006 — wsmprovhost→recon, powershell excluded). **2 false candidates dismissed by measurement**: named-pipe PsExec (Sysmon EID17=1 event → infeasible) and PSEXESVC process (plausible admin use → FP). **PrivEsc 3→5, Lateral Movement 3→4**; 90 detections / 59 techniques / 13-14 tactics (only gap: Resource Development, structurally outside telemetry) | Reinforced kill-chain coverage **without FP debt** (each rule 0-baseline + validated field); +3 four-part playbooks (92 total) | A.8.16, A.8.7 | `87-detection-extra4.sh`; `lookups/mitre-attack.csv`; `lookups/alert-guidance.json`; `docs/mitre-navigator-layer.json` (57 re-run) | **Closed (3)** |

> **Accepted risk (CISO decision, 23/06) — AC-RA-02:** the attack-twin exposure
> analysis (`oms-graph`, AM-21) exposes two facts of **identity design**:
> (a) the management account **`ninjaone`** (RMM) is administrator of **42 hosts** = catastrophic
> single point; (b) the delegated administration accounts **`adm-*`** reach
> **all the crown jewels** in a few hops. **Risk ACCEPTED by the business** (the RMM must
> be able to administer the estate; the `adm-*` are the IT operations tool). **Deviation reported**
> against least privilege / AD tiering (A.5.15, A.8.2). **Compensating measures:** these accounts
> are monitored (UEBA + attack twin re-evaluated daily); the decoys (AM-20)
> are pre-positioned on their paths (an attacker aiming at them hits a decoy before
> the real account). **To be re-evaluated in management review** (PAM/RMM tiering recommended in the longer term).

> **Engineering decision (judgment, 22/06) — supervised FP model NOT trained:** with
> **3 cases / 1 label**, training is deliberately blocked (`fpscore` gating).
> *Fabricating labels to reach the threshold would be a poisoning of the training
> set* — a false "FP" would teach the model to **suppress a real
> detection**. Labels must come from the **real qualification** of cases by
> the analyst (TP/FP disposition at closure, SOC console). Effort therefore focuses on the
> **quality of the features** (above), not on artificial volume.

> **Decisions awaiting CISO go-ahead (management review / controlled change A.8.32)** — each action is *prepared, tested in dry-run, and reversible or with zero immediate impact*; it is executed only after a tracked decision:
> - **DEC-01 — Consolidated retention**: `APPLY=1 ./83-retention-consolidee.sh`. Establishes the single source of retention (values = `POLITIQUE-RETENTION.md`) **and** fixes the `OMNI - FortiManager` routing (graylog_0 → `omni-fortimanager`). *Immediate impact: 0 index deleted (dry-run verified); over time, automatic purge beyond the thresholds (intended behavior).* Irreversible (auto-deletion). A.8.10.
> - **DEC-02 — Kerberos deduplication**: `APPLY=1 ./84-kerberoast-dedup.sh`. Removes 3 redundant alert definitions (safeguard: no deletion if the cluster's canonical alert is absent). *Reversible (re-run `73`).* A.8.16.
> - **DEC-03 — Additional 0-FP tripwires**: deploy `80-detection-extra2.sh` (defender_tamper, schtask_payload, amsi_bypass) then re-run `57` (coverage map) + `14` (dashboards). *Additive (new detections), no removal.* A.8.16.

> **Accepted risk (CISO decision, 18/06) — AC-RA-01:** reuse of the LDAPS
> authentication account `svc_siem` as the AD disabling actor, rather than a
> dedicated response account. **Deviation reported** against least privilege (A.5.18) and
> separation of duties: a highly exposed authentication secret acquires a
> write capability on AD. **Compensating measures in place:** strict denylist
> of protected accounts (admin/service/break-glass/`krbtgt`/`adm-`/`svc_`); execution
> **dry-run by default** (double lock `OMNI_AD_DISABLE_ARM`); **GELF logging**
> of every attempt (`event_source=ad_response`); **human-in-the-loop** invocation
> only (never in automatic response); LDAPS secret **not duplicated** (read from
> `00-vars.env`). **To be re-evaluated in management review** (switch to a dedicated account recommended).

## 3. Method (expected by the Stage 2 auditor, Nov. 2026)
- Each entry carries **date, owner, root cause, action, and verification evidence**; the evidence is the Git commit + the state verified in production ("evidence is evidence").
- The entries above feed the **management review** (clause 9.3) and the **risk register**.
- ISMS vitality indicator: this register shows **dated and verified** entries (≠ frozen scores year over year = auditor red flag).
